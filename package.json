{
  "name": "meal-plan",
  "private": true,
  "version": "1.1.0",
  "description": "2-week meal plan web app with a small Node server that keeps the Claude API key private",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  }
}
